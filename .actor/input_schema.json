{
  "title": "Domain.com.au Property Scraper",
  "description": "Extract Australian real estate listings from Domain.com.au with comprehensive property details, prices, and market data",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Search URL",
      "type": "string",
      "description": "Domain.com.au search URL to scrape properties from. Default searches all properties for sale in Australia.",
      "editor": "textfield",
      "example": "https://www.domain.com.au/sale/",
      "default": "https://www.domain.com.au/sale/",
      "prefill": "https://www.domain.com.au/sale/"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Specific location to search (e.g., 'sydney', 'melbourne', 'brisbane'). Leave empty to use startUrl.",
      "editor": "textfield",
      "example": "sydney-nsw"
    },
    "state": {
      "title": "State",
      "type": "string",
      "description": "Australian state to search in (nsw, vic, qld, wa, sa, tas, act, nt).",
      "editor": "select",
      "enum": ["nsw", "vic", "qld", "wa", "sa", "tas", "act", "nt"],
      "enumTitles": ["New South Wales", "Victoria", "Queensland", "Western Australia", "South Australia", "Tasmania", "Australian Capital Territory", "Northern Territory"]
    },
    "propertyType": {
      "title": "Property Type",
      "type": "string",
      "description": "Filter by property type.",
      "editor": "select",
      "enum": ["house", "apartment", "townhouse", "villa", "land"],
      "enumTitles": ["House", "Apartment/Unit/Flat", "Townhouse", "Villa", "Vacant Land"]
    },
    "minPrice": {
      "title": "Minimum Price (AUD)",
      "type": "integer",
      "description": "Minimum property price in Australian Dollars.",
      "editor": "number",
      "example": 500000,
      "minimum": 0
    },
    "maxPrice": {
      "title": "Maximum Price (AUD)",
      "type": "integer",
      "description": "Maximum property price in Australian Dollars.",
      "editor": "number",
      "example": 1000000,
      "minimum": 0
    },
    "minBeds": {
      "title": "Minimum Bedrooms",
      "type": "integer",
      "description": "Minimum number of bedrooms.",
      "editor": "number",
      "minimum": 0,
      "maximum": 10,
      "example": 3
    },
    "sortBy": {
      "title": "Sort Results By",
      "type": "string",
      "description": "How to sort the search results.",
      "editor": "select",
      "enum": ["dateupdated-desc", "price-asc", "price-desc", "suburb-asc"],
      "enumTitles": ["Date Updated (Newest First)", "Price (Low to High)", "Price (High to Low)", "Suburb (A-Z)"],
      "default": "dateupdated-desc"
    },
    "collectDetails": {
      "title": "Collect Full Property Details",
      "type": "boolean",
      "description": "Visit each property detail page to extract complete descriptions, photos, inspection times, and additional information. Increases runtime but provides comprehensive data.",
      "editor": "checkbox",
      "default": false
    },
    "maxResults": {
      "title": "Maximum Properties",
      "type": "integer",
      "description": "Maximum number of property listings to collect (1-1000).",
      "minimum": 1,
      "maximum": 1000,
      "default": 10,
      "editor": "number"
    },
    "maxPages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process (each page typically contains 20-30 properties).",
      "minimum": 1,
      "maximum": 50,
      "editor": "number",
      "default": 1
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for requests. Residential proxies strongly recommended to avoid blocking.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": []
}
